# Technical reserve

This directory contains the experimental continuous galaxy → system → planet map and WebGPU celestial renderer. It is intentionally not imported by the current game UI; the first playable release uses only the galaxy operations map.

Keep changes here isolated from production components until the three-level map work is resumed. The corresponding design is documented in `docs/technical-reserve/three-level-map.md`.
